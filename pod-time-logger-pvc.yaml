apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pod-startup-pvc
  namespace: pod-time-measure-controller-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi